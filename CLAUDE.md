# OpenClaw Chat - 项目开发规范

本文档记录项目的开发规范和约定。

## 版本管理规范

**项目**: OpenClaw Chat H5
**版本格式**: 语义化版本 (Semantic Versioning)
**格式**: MAJOR.MINOR.PATCH (例如: 1.0.0)

### 版本号规则

1. **MAJOR (第一位)** - 大功能模块
   - 增加大功能模块时递增
   - MINOR 和 PATCH 归零
   - 示例: 1.2.3 → 2.0.0

2. **MINOR (第二位)** - 简单新功能
   - 增加简单功能时递增
   - PATCH 归零
   - 示例: 1.0.9 → 1.1.0

3. **PATCH (最后一位)** - Bug修复
   - 修复bug时递增
   - 示例: 1.0.0 → 1.0.1

### 版本递增示例

| 当前版本 | 变更类型 | 新版本 | 说明 |
|---------|---------|--------|------|
| 1.0.0 | Bug修复 | 1.0.1 | 修复小bug |
| 1.0.1 | Bug修复 | 1.0.2 | 继续修复bug |
| 1.0.2 | 简单功能 | 1.1.0 | 添加小功能，PATCH归零 |
| 1.1.0 | 简单功能 | 1.2.0 | 再添加小功能 |
| 1.2.3 | 大功能模块 | 2.0.0 | 大版本更新，后两位归零 |
| 2.0.0 | 大功能模块 | 3.0.0 | 继续大版本更新 |

### 实施要点

1. **版本号定义位置**: `h5/package.json`
   ```json
   {
     "version": "1.0.0"
   }
   ```

2. **构建时注入**: `h5/vite.config.js` 会自动读取并注入为 `__APP_VERSION__`

3. **显示位置**: 设置页面底部显示当前版本号

4. **commit message**: 版本变更时需在commit message中明确说明版本号变更原因

### 注意事项

- 严格按照语义化版本规范递增版本号
- 不要随意跳版本号（如从 1.0.0 直接跳到 1.2.0）
- 每次修改版本号后必须重新构建 (`npm run build`)
- Bug修复只改PATCH位，不要改MINOR或MAJOR
- 功能增加根据规模判断是MINOR还是MAJOR

**当前版本**: 1.0.0
**起始日期**: 2026-03-27

## 回复语言

- 与用户沟通时，尽量使用中文
- 代码注释和文档使用中文编写（除非是国际化相关内容）
